{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimum permissions for the AEGIS command-center window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
