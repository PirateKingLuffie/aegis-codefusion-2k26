# Android field wrapper — deferred

**Status: paused and outside the active hackathon scope.** Online web and Windows are the current delivery targets. Do not spend demo time rebuilding, installing or presenting the phone client unless the user explicitly resumes it.

The retained Capacitor 8 configuration and native project under `android/` document earlier work and preserve a possible future restart point. No login was added.

A historical unsigned debug APK exists at `../../artifacts/releases/AEGIS-CodeFusion-2K26-debug.apk`. It was not installed or accepted on the target phone, is not a signed release, and is not an active deliverable. See `../../docs/PLATFORM_BUILDS.md` for the preserved status and resume conditions.
