{
  "name": "@aegis/android",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor Android wrapper for the AEGIS field client",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "native:init": "cap add android",
    "native:sync": "cap sync android",
    "android:open": "cap open android",
    "android:run": "cap run android",
    "web:verify": "npm --prefix ../.. run build"
  },
  "dependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0",
    "typescript": "5.9.3"
  }
}
